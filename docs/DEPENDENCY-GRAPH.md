# Ecosystem Dependency Graph

```mermaid
graph TD
  openshell[$(basename OpenShell/pyproject.toml)]
  agent_field[$(basename agent-field/pyproject.toml)]
  autoresearch[$(basename autoclaw/pyproject.toml)]
  cocapn_oneiros[$(basename cocapn-oneiros/pyproject.toml)]
  collective_ai[$(basename collective-ai/pyproject.toml)]
  commit_predictor[$(basename commit-predictor/pyproject.toml)]
  constraint_theory[$(basename constraint-theory-py/pyproject.toml)]
  flux_constraint[$(basename constraint-theory-rust-python/pyproject.toml)]
  device_router[$(basename device-router/pyproject.toml)]
  fleet_calibrator[$(basename fleet-calibrator/pyproject.toml)]
  quartermaster_gc[$(basename fleet-health-monitor/pyproject.toml)]
  quartermaster_gc[$(basename fleet-murmur/pyproject.toml)]
  fleet_router[$(basename fleet-router/pyproject.toml)]
  flux_constraint_check[$(basename flux-check-py/pyproject.toml)]
  flux_genome[$(basename flux-genome-py/pyproject.toml)]
  flux_hyperbolic[$(basename flux-hyperbolic-py/pyproject.toml)]
  flux_lib[$(basename flux-lib-py/pyproject.toml)]
  luciddreamer_agent[$(basename luciddreamer-agent/pyproject.toml)]
  luciddreamer[$(basename luciddreamer/pyproject.toml)]
  micro_onnx[$(basename micro-onnx/pyproject.toml)]
  penrose_memory[$(basename penrose-memory/pyproject.toml)]
  plato_core[$(basename plato-core/pyproject.toml)]
  plato_data[$(basename plato-data/pyproject.toml)]
  plato_escalation_gate[$(basename plato-escalation-gate/pyproject.toml)]
  plato_mcp[$(basename plato-mcp/pyproject.toml)]
  plato_model_ocean[$(basename plato-model-ocean/pyproject.toml)]
  plato_room_intelligence[$(basename plato-room-intelligence/pyproject.toml)]
  plato_training[$(basename plato-training/pyproject.toml)]
  plato_types[$(basename plato-types/pyproject.toml)]
  polyformalism_a2a[$(basename polyformalism-a2a-python/pyproject.toml)]
  quartermaster_gc[$(basename quality-gate-stream/pyproject.toml)]
  sana[$(basename sana-wm/pyproject.toml)]
  sunset_ecosystem[$(basename sunset-ecosystem/pyproject.toml)]
  swarm_rooms[$(basename swarm-rooms/pyproject.toml)]
  tensor_spline[$(basename tensor-spline/pyproject.toml)]
  tile_memory[$(basename tile-memory/pyproject.toml)]
  training_throttle[$(basename training-throttle/pyproject.toml)]
  triplet_miner[$(basename triplet-miner/pyproject.toml)]
  constraint_theory_llvm[$(basename constraint-theory-llvm/Cargo.toml)]
  flux_constraint[$(basename constraint-theory-rust-python/Cargo.toml)]
  dodecet_encoder[$(basename dodecet-encoder/Cargo.toml)]
  eisenstein[$(basename eisenstein/Cargo.toml)]
  fleet_resonance[$(basename fleet-resonance/Cargo.toml)]
  flux_fracture[$(basename flux-fracture/Cargo.toml)]
  flux_lucid[$(basename flux-lucid/Cargo.toml)]
  flux_vm_v3[$(basename flux-vm-v3/Cargo.toml)]
  guardc_v3[$(basename guardc-v3/Cargo.toml)]
  holonomy_consensus[$(basename holonomy-consensus/Cargo.toml)]
  lighthouse_cli[$(basename lighthouse-cli/Cargo.toml)]
  memory_crystal[$(basename memory-crystal/Cargo.toml)]
  neural_plato[$(basename neural-plato/Cargo.toml)]
  openhuman[$(basename openhuman/Cargo.toml)]
  pbft_rust[$(basename pbft-rust/Cargo.toml)]
  penrose_memory[$(basename penrose-memory/Cargo.toml)]
```
