# Ecosystem Synthesis — The Thread Summary

**All ecosystems in SuperInstance follow one grammar: COLLECT → SELECT → COMPILE.**

That's it. That's the whole insight.

Soft Room → Snap → Hard Room. Ethos → Trinity Score → Sunset. Perceiving → Adapting → Compiled. Tiles → RigidFinder → Compiled. Species → Collie → StudBook. Researcher → Teacher/Critic → Distiller. Fragment → Witness → Negative Space.

Fourteen different systems. Zero new ideas — just rediscoveries of the same constraint: **you can't have both maximum connectivity and maximum diversity at the same time.**

The threshold (97.5%, 0.95, hint_level, 0.3 chaos, 65 max_agents) is the only control surface that matters. Everything else is implementation.

**The unified runtime isn't a program. It's a shared understanding of the threshold.**

Right now the ecosystem has:
- ✅ COLLECT (nerve fibers, spawn, soft room, seeds)
- ✅ SELECT (tournament, ranking, snap, pareto frontier)
- ✅ COMPILE (rigid finder, distillation, sunset, seed bank)
- ✅ FEEDBACK (personalization, delta tracker, Hebbian channels)
- ✅ REGULATE (thermal budget, chaos decay, decay factor)
- ❌ A way to show the user the single threshold they need to tune

The next day's work: make the threshold visible, not the implementation.
